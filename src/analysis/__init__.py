"""Tested primitives for this project.

Keep the reusable, testable logic here and the run-order drivers in ``steps/``.
The split matters because ``src/`` is what tests exercise and ``steps/`` is what
a person runs, and those have different review needs.
"""

__all__ = ["criteria", "results"]
